/**
 * Compiler version — kept in one place so PSF Metadata (§14) and any
 * future build-info surfaces share a single source of truth. Bump in
 * lockstep with package.json.
 */
export const COMPILER_VERSION = "0.1.0";
